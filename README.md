# First-AI-vision-model
The model predicts a person’s age from a facial image using a pre-trained image recognition network. First, it learns to map facial features to an approximate age, then fine-tunes part of the network to improve accuracy. It applies small variations to images to generalize better and finally saves the trained model for future predictions.
